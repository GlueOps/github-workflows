name: "auto-release"
on:
  workflow_call:

jobs:
  auto-release:
    runs-on: ubuntu-latest
    steps:
      - uses: cloudposse/github-action-auto-release@c742e7af1cfd0dd8a63e8947d334a6ae56fbf7f4 # v3
        with:
          publish: false
