name: "auto-release"
on:
  workflow_call:

jobs:
  auto-release:
    runs-on: ubuntu-latest
    steps:
      - uses: cloudposse/github-action-auto-release@a027682afd0ae97f6088453b3e00eaab552633b2 # v2
        with:
          publish: false
